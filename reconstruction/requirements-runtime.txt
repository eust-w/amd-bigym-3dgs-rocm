av>=12,<17
mujoco>=3.1,<4
numpy>=1.26,<3
pillow>=10,<13
scipy>=1.12,<2

# Install the GPU-specific PyTorch build separately from the vendor's index.
# The pinned experiment used gsplat source revision documented in config/reference.env.example.
